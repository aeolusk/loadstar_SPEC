# 📋 LOADSTAR Standard Schema Definitions

## 1. Status Codes

> **적용 대상: WayPoint.** Map과 Data WayPoint(dwp)는 STATUS를 갖지 않는다. 진행 상태는 WayPoint가 단일 출처(Single Source)이며, Map은 산하 WP/dwp 목록을 관리하는 인덱스 역할, dwp는 데이터의 개념적 자기소개로 작업 단위가 아니다. (`05.ELEMENT_FORMAT.md` 참조)

- `S_IDL`: Idle (대기)
- `S_PRG`: In Progress (진행 중)
- `S_STB`: Stable (완료/안정)
- `S_ERR`: Error (오류 발생)
- `S_REV`: Review Required (검토 필요)
- `S_OOS`: Out of Scope (범위 제외 — 초기 계획에 포함됐으나 구현하지 않기로 확정)

## 2. Link Types
- `L_REF`: Reference (참조)
- `L_SEQ`: Sequential (선후 관계)
- `L_TST`: Test Relation (검증 관계)

## 3. TODO Status Codes
- `PENDING`: 대기 중 (착수 전)
- `ACTIVE`: 진행 중
- `BLOCKED`: 선행 조건(`Depends_On`) 미완료로 착수 불가
- `COMPLETED`: 완료 (TODO_LIST에서 즉시 삭제)
- `FAILED`: 실패 (원인 기록 후 재등록 또는 취소 결정)

## 4. OPEN_QUESTIONS 상태
- `[Q{N}]`: 미결 — AI는 해당 항목이 있으면 작업 착수 전 사람에게 확인을 요청한다.
- `[Q{N} DEFERRED]`: 보류 — 결정을 당장 내릴 수 없거나 뒤로 미룬 상태. CLI `question` 출력에는 표시되지만 차단 요소로는 취급되지 않는다.
- `[Q{N} CONFIRMED]` 또는 `[Q{N} CONFIRMED <DECISION_ID>]`: 확인 완료 — 사람이 결정을 승인한 상태. 기본 출력에서 제외된다(`question --all`로 조회). 설계적 판단은 Decision 파일로 기록하고 ID를 표기하며, 단순 Q&A는 인라인으로만 처리해도 된다. 구분 기준은 `05.ELEMENT_FORMAT` Decision 섹션 참조.

**형식 예시**
```
### ISSUE
- OPEN_QUESTIONS:
  - [Q1] MAP 삭제 시 그 맵 산하 모든 WP를 어떻게 처리할 것인가?
  - [Q2 DEFERRED] 차트 라이브러리 최종 선택 — 성능 벤치 후 재검토
  - [Q3 CONFIRMED orphan_delete.2026-04-24.001] Block + --cascade 플래그
  - [Q4 CONFIRMED] 인라인으로 합의된 단순 결정
```

**규약**
- `<DECISION_ID>`는 `.loadstar/DECISIONS/` 하위 파일명에서 확장자(`.md`)를 뺀 형태.
- Decision 파일 포맷은 `05.ELEMENT_FORMAT.md` Decision 섹션 참조.
- 한 Decision이 여러 WP에 영향을 주면, 각 WP의 해당 Q 라인 모두 같은 DECISION_ID를 참조한다.

## 5. TODO 체크박스 규칙
- `- [ ] 항목`: 미완료
- `- [x] YYYY-MM-DD 항목`: 완료 (완료 날짜 기입)
- AI 재진입 시 체크박스 상태로 중간 진행 위치를 즉시 파악한다.
- 체크박스 업데이트는 `loadstar edit` 또는 직접 편집으로 수행하며, 변경 이력이 Git에 자동 기록된다.
- 완료 항목이 과다하게 누적된 경우, WayPoint 아카이브 파일로 분리할 수 있다 (`05.ELEMENT_FORMAT.md` 참조).

## 6. SYNCED_AT 규칙
- WayPoint IDENTITY 및 Map IDENTITY에 포함되는 날짜 필드.
- **의미**: 이 요소의 내용이 실제 코드/상태와 마지막으로 일치함을 확인한 날짜.
- **갱신 시점**:
  - WayPoint: TODO 항목 체크, CODE_MAP 갱신, 또는 STATUS 변경 시
  - Map: 하위 요소 구성이 변경될 때
- **AI 행동 규칙**: SYNCED_AT이 현재 날짜 기준 30일 이상 지난 WayPoint는 CODE_MAP scope의 유효성을 재확인한 후 작업한다.
- 형식: `YYYY-MM-DD HH:mm` (시/분 포함. 하위 호환: 기존 `YYYY-MM-DD` 형식도 유효)

## 7. Attachment URL Schemes (v1.7.0)

WP/dwp의 `ATTACHMENTS` 슬롯에 적는 외부 자료는 표준 URL 스킴 표기를 사용한다. LOADSTAR 그래프 외부의 파일·문서·URL을 1급 자료로 매다는 표준 자리이며, 표기 규격만 정의한다(존재성·도달성 검증은 본 버전 범위 밖).

### 7.1 허용 스킴

| 스킴 | 용도 | 형식 |
|:---|:---|:---|
| `https://` / `http://` | 외부 URL (위키, 문서 사이트, 이슈, S3 객체 등) | `https://<host>/<path>` — 표준 RFC 3986 그대로 |
| `file://` | 프로젝트 내 로컬 파일 (SQL, 이미지, PDF, 마이그레이션 스크립트 등) | `file:///<프로젝트 루트 기준 상대경로>` |

### 7.2 `file://` 경로 규약

- 형태: `file:///<path>` — 슬래시 3개 후 경로.
- 경로 기준: **프로젝트 루트 기준 상대경로**. OS별 절대경로(C:\..., /Users/...)는 사용하지 않는다.
- 경로 구분자: `/` (Windows에서도 동일).
- 인코딩: 공백·한글 등은 가독성을 위해 그대로 적되, 필요 시 URL 인코딩(`%20` 등)을 사용해도 무방하다.

> **RFC와의 차이**: RFC 8089의 `file:///path`는 OS 절대경로 의미지만, LOADSTAR는 이를 **프로젝트 루트 기준 상대경로**로 재정의한다. CLI/UI가 자료를 열 때는 LOADSTAR 프로젝트 루트(`.loadstar/`의 상위 디렉토리)를 기준으로 해석한다.

### 7.3 설명 표기 (선택)

각 항목은 URL 한 줄로 끝낼 수도 있고, ` — `(스페이스+엠대시+스페이스)로 한 줄 설명을 덧붙일 수도 있다.

```
### ATTACHMENTS
- https://example.com/spec.pdf
- file:///backend/db/migrations/V12__init.sql
- file:///docs/design/auth_flow.png — 인증 흐름 다이어그램
```

설명은 의무가 아니며, UI/CLI가 자료 목록을 표시할 때 라벨로 활용한다.

### 7.4 향후 확장 (참고)

- 검증: `loadstar validate`에서 `file://` 경로 존재 여부, `https://` 도달 여부 등은 후속 버전에서 도입한다.
- 추가 스킴(`mailto:`, `git+ssh://`, `s3://` 등)은 필요 시 본 표에 항목을 추가하며, 표 외 스킴은 일단 free-form URL로 취급한다.

---

## 8. GD 슬롯 — Good Dopamin (v1.8.0)

> ⚠️ **미구현**: 본 섹션은 SPEC 정의만 완료된 상태입니다. `loadstar gd` CLI 및 `.loadstar/GD/` 디렉토리 초기화는 아직 구현되지 않았습니다. 추후 `loadstar_cli` 프로젝트에서 구현 예정입니다.

`.loadstar/GD/`는 프로젝트 맥락을 Q&A 형태로 압축 유지하는 슬롯이다. AI가 세션 진입 시 참고하는 거친 맥락 요약본으로, 용량 제한과 점수 기반 decay/boost로 항목의 신선도를 자동 관리한다.

> **명칭 이유**: 강화(boost)와 망각(decay) 메커니즘을 도파민에 빗댄 이름. `GD-001` 형태의 항목 ID로 AI 대화 중 즉각 식별 가능하다.

### 8.1 파일 구조

```
.loadstar/GD/
├── context.md    # 활성 항목 (기본 크기 제한: 32 KB)
└── archive.md    # 삭제된 항목 (기본 크기 제한: 16 KB, FIFO 삭제)
```

- 크기 제한은 `.loadstar/COMMON/gd_config.toml`에서 재정의할 수 있다 (없으면 기본값 사용).
- `archive.md`가 크기 제한을 초과하면 가장 오래된 항목부터 영구 삭제한다.

### 8.2 항목 스키마

`context.md`의 각 항목은 다음 형식을 따른다.

```markdown
## GD-{NNN}
Q: {질문}
A: {답변. WayPoint/파일 링크 포함 가능 — [→W://address] 또는 [→file:///path]}
s:{점수} c:{답변변경횟수} qc:{질문변경횟수} rv:{복귀횟수}
```

| 필드 | 의미 | 비고 |
|:--|:--|:--|
| `s` | 현재 점수 (0 이하 시 삭제 대상) | 정수 |
| `c` | 답변(A) 변경 횟수 | 갱신마다 +1 |
| `qc` | 질문(Q) 변경 횟수 | 갱신마다 +1 |
| `rv` | 아카이브에서 복귀한 횟수 | 복귀마다 +1 |

- Q와 A는 모두 변경 가능하다. 변경 이력은 파일 내 보관하지 않으며 git이 관리한다.
- ID(`GD-{NNN}`)는 항목 생성 시 순번 부여, 이후 불변.

**예시**

```markdown
## GD-003
Q: UI 구현 시 SOAP 연동 목표는?
A: WS-Security 기반 인증. WSDL 위치 → [→file:///docs/soap.wsdl], 구현 WP → [→W://root/ui/api]
s:24 c:3 qc:1 rv:0
```

### 8.3 점수 정책 (고정값, v1.8.0)

| 이벤트 | 점수 변화 |
|:--|:--|
| 신규 항목 생성 | +30 (초기값) |
| 세션 tick (decay) | -1 |
| 당일 작업 태그 매칭 (boost) | +2 |
| 아카이브 복귀 | +5 × (rv + 1) |

- **삭제 조건**: `s ≤ 0` 이거나 `context.md` 크기 초과 시 점수 최저 항목부터 삭제.
- **태그 매칭**: 답변에 포함된 WayPoint 주소(`W://`) 또는 항목에 명시된 태그가 당일 작업 WP와 겹치면 boost 적용.
- 점수 정책 파라미터는 향후 `pulse_config.toml`로 외부화 예정 (현재는 고정).

### 8.4 아카이브 스키마

`archive.md`에는 삭제된 항목이 그대로 보존된다. 추가 필드 없이 `context.md`와 동일한 스키마를 사용하며, 항목 말미에 삭제 시점 점수가 기록된 채로 남는다.

### 8.5 운영 규칙

- **세션 진입 시**: AI는 `context.md`를 읽어 맥락을 보완한다. 필수가 아니며 사용자가 원할 때 참조.
- **세션 종료 시**: `loadstar gd tick`을 실행해 decay를 적용하고, 당일 작업 WP 기반으로 boost를 계산한다.
- **직접 편집 금지**: `context.md`와 `archive.md`는 `loadstar gd` CLI로만 수정한다. 직접 편집 시 점수/횟수 정합성이 깨진다.
- **git 이력**: GD 파일도 `.loadstar/` 하위이므로 git 추적 대상이다. 변경 이력은 git으로 관리한다.